node_modules/
dist/
.aws-sam/
lambda.zip
samconfig.toml
*.log

# Secrets. frontend/.env.production is committed on purpose: it holds only the
# public API URL, and frontend code is public anyway.
.env
.env.local
.env.aws
.env.deploy
.cf-token
.wrangler/
frontend/.wrangler/

# 含远程 MCP 端点与访问令牌，仓库是公开的，绝不能入库
.mcp.json
