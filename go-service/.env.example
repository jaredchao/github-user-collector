# 本地经 SSM 隧道连 RDS。隧道把 RDS:5432 转发到本地 5433。
# 阶段 2 容器里改为 RDS 真实端点 ...rds.amazonaws.com:5432。
# sslmode=require 必需：RDS 强制 SSL，经隧道连 localhost 证书主机名不匹配。
DATABASE_URL=postgres://postgres:PASSWORD@localhost:5433/github_users?sslmode=require
PORT=8080
