VITE_GO_API_URL=https://main.go-api.jccode.cc

# Real-user monitoring. Committed for the same reason as the line above: it
# is a public, write-only endpoint that every visitor's browser calls anyway.
# PR previews override this to empty, so a throwaway environment does not mix
# its numbers into the production site's data.
VITE_INGEST_URL=https://1p4i4rhooj.execute-api.us-east-2.amazonaws.com/v1/collect
